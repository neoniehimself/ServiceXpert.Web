@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h2 class="text-danger">Error</h2>
@if (ViewData["IsErrorFromController"] == null)
{
    <p class="text-danger">@Model.ErrorString</p>
}
else
{
    if ((bool)ViewData["IsErrorString"]!)
    {
        <p class="text-danger">@Model.ErrorString</p>
    }
    else
    {
        <ul class="text-danger">
            @foreach (var error in Model.ErrorList)
            {
                <li>@error</li>
            }
        </ul>
    }
}